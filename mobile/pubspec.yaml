name: attendance_mobile
description: App móvil para marcar entrada y salida con reconocimiento facial.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  camera: ^0.10.6
  google_mlkit_face_detection: ^0.11.0
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
